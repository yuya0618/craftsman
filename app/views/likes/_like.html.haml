-# いいね機能
-if user_signed_in?
  -# ログイン＋いいね済み
  -# -binding.pry
  -if current_user.already_liked?(build)
    %li
      = link_to "", build_like_path(like, build_id: build.id),method: :delete, id:"like-button", class:"btn fas fa-star",remote: true, data: { confirm: "nice!!を削除します。よろしいですか？" }
    %li
      %span
        = build.likes.count
      nice!!
    -# ログイン＋未いいね
  -else
    %li
      = link_to "", build_likes_path(build_id: build.id), id:"like-button",method: :post, class:"btn far fa-star",remote: true
    %li
      %span
        = build.likes.count
      nice!!
  -# ログインしてないとき
-else
  %li
    = icon 'fas', 'fa-star'
  %li
    %span
      = build.likes.count
    nice!!